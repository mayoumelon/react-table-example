import type { FC } from 'react';
import PermissionSettings from 'components/templates/PermissionSettings';

const PermissionUpdate: FC = () => {
  return <PermissionSettings action="update" />;
};

export default PermissionUpdate;
